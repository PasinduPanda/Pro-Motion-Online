{
  "name": "pro-motion-root",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install && npx prisma generate && npx prisma db push --accept-data-loss",
    "start": "cd backend && npm start"
  }
}
